version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "migrations"
    gen:
      go:
        package: "database"
        emit_json_tags: true
        out: "."
        sql_package: "pgx/v5"
